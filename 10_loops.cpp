#include <iostream>
using namespace std;

int main()
{
    /*there are three types of loop
        1. For lopp
        Syntax for for loop

       for(initialization; condition; updation)
       {
           loop body(C++ code);
       }
        2. While loop

         // Syntax:
        while(condition)
       {
            C++ statements;
        }

        3.do-whilw loo

        Syntax:
         do
         {
             C++ statements;
         }while(condition);
    */
    return 0;
}